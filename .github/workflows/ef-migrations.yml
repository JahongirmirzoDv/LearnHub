name: EF Core migration (cloud)

# Generates a new EF Core migration in the cloud and commits it to the selected branch. Useful for team members
# who do not have the .NET SDK installed locally. The project uses a single provider (SQLite), so one migration
# per name is produced, in the same folder the application loads migrations from at start-up.
on:
  workflow_dispatch:
    inputs:
      name:
        description: "Migration name in PascalCase, e.g. AddCourseLevel"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: ef-migrations-${{ github.ref }}
  cancel-in-progress: false

env:
  DOTNET_NOLOGO: "true"
  DOTNET_CLI_TELEMETRY_OPTOUT: "true"

jobs:
  add-migration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out branch
        uses: actions/checkout@v7

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"

      - name: Validate migration name
        env:
          MIGRATION_NAME: ${{ inputs.name }}
        run: |
          if [[ ! "$MIGRATION_NAME" =~ ^[A-Z][A-Za-z0-9]{2,80}$ ]]; then
            echo "::error::Migration name must be PascalCase letters/digits (3-81 chars)."
            exit 1
          fi

      - name: Restore tools and packages
        run: |
          dotnet tool restore
          dotnet restore LearnHub.sln

      - name: Add migration
        env:
          MIGRATION_NAME: ${{ inputs.name }}
        run: >
          dotnet ef migrations add "$MIGRATION_NAME"
          --project src/LearnHub
          --output-dir Data/Migrations

      - name: Verify the solution still builds
        run: dotnet build LearnHub.sln --configuration Release

      - name: Commit migration
        env:
          MIGRATION_NAME: ${{ inputs.name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/LearnHub/Data/Migrations
          if git diff --cached --quiet; then
            echo "No migration changes to commit."
            exit 0
          fi
          git commit -m "chore(db): add $MIGRATION_NAME migration"
          git push
